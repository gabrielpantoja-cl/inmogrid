/**
 * Primitivos compartidos para el menú de cuenta (dropdown + modal de delete
 * + hook de estado). Usados tanto por el navbar del dashboard como por el
 * `PublicHeader` de las rutas públicas.
 */
export { AccountMenu } from './AccountMenu';
export { DeleteAccountDialog } from './DeleteAccountDialog';
export { useAccountActions } from './useAccountActions';
