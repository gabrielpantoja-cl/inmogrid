Fix the GitHub issue described below.

## Instructions

1. Read the issue description: $ARGUMENTS
2. Understand the current code involved — read the relevant files before changing anything
3. Implement the fix with minimal changes (no unrelated refactoring)
4. Run `npm run build` to verify no TypeScript or build errors
5. Run `npm run lint` to check for linting issues
6. If tests exist for the affected area, run `npm run test`
7. Show the diff and propose a conventional commit message (`fix:`, `feat:`, etc.)
8. Do NOT commit or push — just present the changes for review
